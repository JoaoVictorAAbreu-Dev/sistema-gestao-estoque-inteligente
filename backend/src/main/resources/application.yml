spring:
  application:
    name: sistema-gestao-estoque-inteligente
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
app:
  jwt:
    secret: ${JWT_SECRET:change-me-change-me-change-me-change-me}
    expiration-minutes: ${JWT_EXPIRATION_MINUTES:60}
