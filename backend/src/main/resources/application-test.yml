spring:
  datasource:
    url: jdbc:h2:mem:estoque_inteligente;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  flyway:
    enabled: false
app:
  jwt:
    secret: test-secret-test-secret-test-secret-test-secret
    expiration-minutes: 60
